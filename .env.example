# Shared secret — Strapi webhook must send: Authorization: Bearer <this value>
RELAYER_SHARED_SECRET=

# Full Deploy Hook URLs (Vercel project → Settings → Git → Deploy Hooks)
VERCEL_DEPLOY_HOOK_KIPRA=
VERCEL_DEPLOY_HOOK_GEKO=
VERCEL_DEPLOY_HOOK_ALPRA=
